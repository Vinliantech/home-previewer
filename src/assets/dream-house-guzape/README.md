# Dream House in Guzape — Kenneth Minimah Crescent

Drop the renders for this property straight into this folder. They are picked up
automatically: no code change, no import to add.

- **Accepted:** `.jpg`, `.jpeg`, `.png`, `.webp`
- **Order:** filenames are sorted naturally, so name them `01-…`, `02-…`, `03-…`
  to control the sequence. The **first** image is used as the cover on the
  properties grid and at the top of the detail page.
- **Empty is safe:** with no images here the property falls back to
  `src/assets/guzape-dream-homes.jpg` and the gallery section is hidden.

Currently placed (from Documents/New Kaysteph/kay — the four source files were
two unique renders, deduplicated):

```
01-courtyard.jpg          # courtyard view, dark-panel terraces  →  CARD COVER
02-corner-elevation.jpg   # tall corner block with the vertical window slot
```

The brick-accent daytime elevation once shown for this project is actually
"Karsana Fully Detached 1" and is used as the Lillycrest Residence cover — it
was not placed here to avoid the same image fronting two different projects.
Drop a dedicated Guzape render named `00-…` here to replace the cover.

Keep each file under ~400 KB where possible — these are shipped to every
visitor. Export at roughly 1600 px on the long edge.
